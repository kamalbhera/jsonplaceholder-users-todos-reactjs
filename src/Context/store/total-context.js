// ! here created a context - global store

import React, { createContext } from "react";
const TotalContext = React.createContext(0);
export default TotalContext;

//! Now lets create the provider(component)

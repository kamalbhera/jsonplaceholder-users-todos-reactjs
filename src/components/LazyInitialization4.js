/* function initialState() {
  console.log("Expensive Call process", Date.now());
  return 0;
}

export default function App() {
  const [count, setCount] = useState(() => initialState()); // here we are using  lazy initialization process.
} */
